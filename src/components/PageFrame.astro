---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;

const deafultClass =
  "mx-auto grid min-h-screen w-full grid-cols-1 lg:grid-cols-[18rem_minmax(0,1fr)_18rem]";
const classes = [deafultClass, className];
---

<div class:list={[classes]}>
  <slot />
</div>
